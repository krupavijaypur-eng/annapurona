import type { InventoryItem, ShoppingListItem } from '@/lib/types';

// This file now only serves as a fallback or for initial type definition reference.
// The main state is managed in AppContext.tsx and persisted to localStorage.

export const mockInventory: InventoryItem[] = [];

export const mockShoppingList: ShoppingListItem[] = [];
